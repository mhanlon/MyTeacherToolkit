import XCTest
@testable import MyTeacherToolKit

final class MyTeacherToolKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MyTeacherToolKit().text, "Hello, World!")
    }
}
